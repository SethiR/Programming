// To run a scala program just do scala filename.scala

object ScalaTutorial{
    def main(args: Array[String]){

            var name = "Rajat Sethi"

            for (i<-0 until name.length){
                println(name(i))
            }

        }
    }